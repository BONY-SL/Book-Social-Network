spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_network
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: diphlk
    password: diphlk
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enable: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

application:
  security:
    jwt:
      secret-key: Wjrnrx5SJWbdoMig42xh7RTvmPxmXO4pdyX1SjJWY+I=
      expiration: 3600000
  mailing:
    frontend:
      activation-url: http://localhost:4200/activate
  file:
    upload:
      photos-output-dir: ./uploads

server:
  port: 8080